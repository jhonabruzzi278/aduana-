<section class="my-6 bg-white p-4 rounded shadow">
  <h3 class="text-lg font-semibold mb-4">Consulta de Trámite</h3>
  <a href="/src/pages/turista/">Volver al inicio </a>

  <form id="form-consulta" class="space-y-4">
    <div>
      <label for="codigo" class="block font-medium">Código de trámite</label>
      <input id="codigo" type="text" class="w-full p-2 border border-gray-300 rounded" />
    </div>
    <button type="submit" class="bg-gray-700 text-white px-4 py-2 rounded hover:bg-gray-600 w-full">
      Consultar
    </button>
  </form>
</section>


<script type="module">
 
  window.addEventListener("DOMContentLoaded", () => {
   
    import("/scripts/formularioConsultaTramite.js")
      .then((module) => {
        
        if (module.setupFormulario) {
          module.setupFormulario();
        } else {
          console.warn("El módulo formularioConsultaTramite.js no exporta 'setupFormulario'.");
        }
      })
      .catch((error) => {
        console.error("Error al cargar el script formularioConsultaTramite.js:", error);
      });
  });
</script>