<section class="my-6 bg-white p-4 rounded shadow">
  <h3 class="text-lg font-semibold mb-4">Ingreso de Pasajero</h3>

  <form id="form-pasajero" class="space-y-4">
    <div>
      <label for="nombre" class="block font-medium">Nombre completo</label>
      <input id="nombre" type="text" class="w-full p-2 border border-gray-300 rounded" />
    </div>
    <div>
      <label for="documento" class="block font-medium">Documento</label>
      <input id="documento" type="text" class="w-full p-2 border border-gray-300 rounded" />
    </div>
    <div>
      <label for="nacionalidad" class="block font-medium">Nacionalidad</label>
      <input id="nacionalidad" type="text" class="w-full p-2 border border-gray-300 rounded" />
    </div>
    <button type="submit" class="bg-blue-700 text-white px-4 py-2 rounded hover:bg-blue-600 w-full">
      Registrar ingreso
    </button>
  </form>
</section>


<script type="module">
 
  window.addEventListener("DOMContentLoaded", () => {
   
    import("/scripts/formularioPasajero.js")
      .then((module) => {
        
        if (module.setupFormulario) {
          module.setupFormulario();
        } else {
          console.warn("El módulo formularioPasajero.js no exporta 'setupFormulario'.");
        }
      })
      .catch((error) => {
        console.error("Error al cargar el script formularioPasajero.js:", error);
      });
  });
</script>