<section class="my-6 bg-white p-4 rounded shadow">
  <h3 class="text-lg font-semibold mb-4">Salida de Pasajero</h3>

  <form id="form-salida" class="space-y-4">
    <div>
      <label for="documento-salida" class="block font-medium">Documento del pasajero</label>
      <input id="documento-salida" type="text" class="w-full p-2 border border-gray-300 rounded" />
    </div>
    <button type="submit" class="bg-red-600 text-white px-4 py-2 rounded hover:bg-red-500 w-full">
      Registrar salida
    </button>
  </form>
</section>


<script type="module">
  
  window.addEventListener("DOMContentLoaded", () => {
   
    import("../scripts/formularioPasajeroSalida.js")
      .then((module) => {
        
        if (module.setupFormularioSalida) {
          module.setupFormularioSalida();
        } else {
          console.warn("El módulo formularioPasajeroSalida.js no exporta 'setupFormularioSalida'.");
        }
      })
      .catch((error) => {
        console.error("Error al cargar el script formularioPasajeroSalida.js:", error);
      });
  });
</script>