<section class="my-6 bg-white p-4 rounded shadow">
  <h3 class="text-xl font-bold mb-4">📊 Resumen de Trámites</h3>

  <div id="resumen-contenido">
    <p class="text-gray-600">Cargando datos desde el navegador...</p>
  </div>
</section>

<script type="module">
  window.addEventListener("DOMContentLoaded", () => {
    import("/scripts/resumenTramites.js")
      .then((module) => {
        if (module.setupResumenTramites) {
          module.setupResumenTramites();
        } else {
          console.warn("El módulo no exporta 'setupResumenTramites'");
        }
      })
      .catch((err) => {
        console.error("No se pudo cargar resumenTramites.js", err);
      });
  });
</script>

