---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="Página no encontrada - 404">
  <section class="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-50 to-indigo-100 px-4">
    <div class="text-center max-w-2xl mx-auto">
      <!-- Número 404 animado -->
      <div class="relative mb-8">
        <h1 class="text-9xl md:text-[12rem] font-black text-transparent bg-clip-text bg-gradient-to-r from-blue-600 to-purple-600 animate-pulse">
          404
        </h1>
        <div class="absolute inset-0 text-9xl md:text-[12rem] font-black text-blue-200 blur-sm -z-10">
          404
        </div>
      </div>

      <!-- Mensaje principal -->
      <div class="mb-8 space-y-4">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-800 mb-4">
          ¡Oops! Página no encontrada
        </h2>
        <p class="text-lg text-gray-600 mb-2">
          Parece que la página que buscas se perdió en el espacio digital.
        </p>
        <p class="text-base text-gray-500">
          Puede que haya sido movida, eliminada o que la URL sea incorrecta.
        </p>
      </div>

      <!-- Ilustración SVG -->
      <div class="mb-8">
        <svg class="w-64 h-64 mx-auto text-blue-300" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
          <circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="1.5" fill="none" opacity="0.3"/>
          <path d="M8 14s1.5 2 4 2 4-2 4-2" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          <circle cx="9" cy="9" r="1.5" fill="currentColor"/>
          <circle cx="15" cy="9" r="1.5" fill="currentColor"/>
        </svg>
      </div>

      <!-- Botones de acción -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <a 
          href="/" 
          class="inline-flex items-center px-8 py-3 bg-gradient-to-r from-blue-600 to-blue-700 text-white font-semibold rounded-lg hover:from-blue-700 hover:to-blue-800 transform hover:scale-105 transition-all duration-200 shadow-lg hover:shadow-xl"
        >
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
          </svg>
          Volver al inicio
        </a>
        
        <button 
          onclick="history.back()" 
          class="inline-flex items-center px-8 py-3 bg-gray-200 text-gray-700 font-semibold rounded-lg hover:bg-gray-300 transform hover:scale-105 transition-all duration-200"
        >
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
          </svg>
          Página anterior
        </button>
      </div>

      <!-- Enlaces útiles -->
      <div class="mt-12 pt-8 border-t border-gray-200">
        <p class="text-gray-500 mb-4">¿Necesitas ayuda? Prueba estos enlaces:</p>
        <div class="flex flex-wrap justify-center gap-4 text-sm">
          <a href="/contacto" class="text-blue-600 hover:text-blue-800 hover:underline">Contacto</a>
          <span class="text-gray-300">•</span>
          <a href="/ayuda" class="text-blue-600 hover:text-blue-800 hover:underline">Centro de ayuda</a>
          <span class="text-gray-300">•</span>
          <a href="/sitemap" class="text-blue-600 hover:text-blue-800 hover:underline">Mapa del sitio</a>
        </div>
      </div>
    </div>
  </section>

  <style>
    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-10px); }
    }
    
    .animate-float {
      animation: float 3s ease-in-out infinite;
    }
  </style>

  <script>
    // Añadir animación flotante al SVG
    document.addEventListener('DOMContentLoaded', () => {
      const svg = document.querySelector('svg');
      if (svg) {
        svg.classList.add('animate-float');
      }
    });
  </script>
</BaseLayout>
